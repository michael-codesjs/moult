import { Command } from 'commander'

describe('CLI', () => {
  it('should initialize correctly', () => {
    // This is a placeholder test that can be expanded as needed
    const program = new Command()
    expect(program).toBeDefined()
  })
})
